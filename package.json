{
  "name": "pf2e-creature-forge",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check scripts/main.js && find scripts -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
